import unittest
import tempfile
import shutil
import os
from operations import (
     copy_file,
     delete_path,
     count_files_in_dir,
     find_files_in_dir,
     add_date_to_files,
     _add_date_to_filename
)